# Python dependencies for the HTTP API (FastAPI + uvicorn). Canonical list used by:
#   - local runs: pip install -r requirements-api.txt (see ai/src/api_server.py)
#   - Databricks Apps: via requirements.txt, which includes this file
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
# Full `mlflow` is not used (only `mlflow.deployments`), and it conflicts with Apps’
# pre-installed `mlflow-skinny==2.16.2`, often breaking `pip install` before vectorsearch.
mlflow-skinny>=2.11.3,<3
# 0.67+ requires mlflow-skinny 3.x; Apps stays on skinny 2.x for now.
databricks-vectorsearch>=0.50,<0.67
PyYAML>=6.0
