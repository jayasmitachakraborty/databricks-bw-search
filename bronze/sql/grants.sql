-- Example: grant read on bronze objects to a silver transformation service principal.
-- Replace catalog, schema, and principal names.

-- GRANT SELECT ON TABLE `${catalog}`.`${schema}`.`example_table` TO `silver_sp`;
